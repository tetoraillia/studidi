<h1>Bookmarked Courses</h1>

<% @bookmarks.each do |bookmark| %>
    <%= bookmark.course.title %>
    <%= link_to "View", course_path(bookmark.course) %>
    <%= button_to "Unbookmark", bookmark_path(bookmark), method: :delete %>
<% end %>

<%= link_to "Back to Courses", courses_path %>